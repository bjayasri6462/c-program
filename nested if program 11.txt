#include <stdio.h>

int main() {
    int i, j;

    for (i = 5; i >= 1; i--) {
        char ch = 'A' + i - 1;   // E, D, C, B, A
        for (j = i; j <= 5; j++) {
            printf("%c", ch);
            ch++;
        }
        printf("\n");
    }

    return 0;
}